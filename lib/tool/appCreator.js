/**
 * ------------------------------------------------------------------
 * 目前仅为简单返回Koa app功能。
 * 保留Koa app生成逻辑更改与扩展的可能
 * ------------------------------------------------------------------
 */

const Koa = require('koa');

module.exports = () => new Koa();
